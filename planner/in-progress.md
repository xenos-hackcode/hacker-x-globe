# In progress

**Nothing is actively being worked on right now.** The last piece of work
(three follow-ups from `left-to-do.md`: the `imePadding` sweep confirmation,
rejoin-cooldown cleanup in `deleteGroupFully`, and cursor-based pagination
for `getGroupMessages`) shipped and compiled clean on both server and
Android. Idle, waiting on either a device-testing pass against
[not-tested.md](not-tested.md) or new direction from the user.
