# In progress

**Nothing is actively being worked on right now.** The last piece of work
("Known" calling round 1 - Call tab, DM/group Call buttons, opt-in number
sharing, Creator-only group-call lock - see `done.md`'s 2026-08-08 entry)
shipped and compiled clean on both server and Android. "Secretive" (in-app
data/video calling) is deliberately not started - see `left-to-do.md`, it
needs a WebRTC signaling/TURN-cost decision first. Idle, waiting on either
a device-testing pass against [not-tested.md](not-tested.md) or new
direction from the user.
